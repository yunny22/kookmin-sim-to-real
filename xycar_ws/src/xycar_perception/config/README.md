# Calibration scope

`camera_perception.yaml` is the retained simulation-oriented canonical-road
configuration. Real-camera fisheye calibration and real-track calibration
profiles are excluded because they are vehicle-specific measurement artifacts.
Use a separately reviewed calibration file through the launch argument when
running against a real camera.
